import React from 'react'
import {AppBar, Toolbar} from "@mui/material"

export default function Header() {
    return (
        <div>
            <AppBar>
                <Toolbar>
                    
                </Toolbar>
            </AppBar>
        </div>
    )
}
